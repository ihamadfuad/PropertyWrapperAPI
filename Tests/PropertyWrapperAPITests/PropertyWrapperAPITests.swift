import XCTest
@testable import PropertyWrapperAPI

final class PropertyWrapperAPITests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(PropertyWrapperAPI().text, "Hello, World!")
    }
}
